# Working Buffer (Danger Zone Log)

**Status:** INACTIVE

Use this only when context is getting long (danger zone). When activated, append each human message + a short summary of the agent response so the thread survives compaction.
